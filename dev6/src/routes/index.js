import { NavigationContainer } from "@react-navigation/native";
import { Stack } from "./stack";

export const Routes = () => {
  return (
    <NavigationContainer>
      <Stack />
    </NavigationContainer>
  );
};